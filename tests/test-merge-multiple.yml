name: Test download-artifact merge-multiple option
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    env:
      GITHUB_REF: refs/heads/main
      GITHUB_EVENT_NAME: push
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - id: download
        uses: .
        with:
          path: /tmp/merged-artifacts
          merge-multiple: 'true'
        continue-on-error: true

      - name: Assert action ran with merge-multiple flag
        env:
          DOWNLOAD_PATH: ${{ steps.download.outputs.download-path }}
          OUTCOME: ${{ steps.download.outcome }}
        run: |
          echo "outcome: $OUTCOME"
          echo "download-path: $DOWNLOAD_PATH"
          # If the action succeeded, verify the download-path matches the specified path
          if [ "$OUTCOME" = "success" ]; then
            if [ "$DOWNLOAD_PATH" != "/tmp/merged-artifacts" ]; then
              echo "FAIL: expected download-path '/tmp/merged-artifacts' but got '$DOWNLOAD_PATH'"
              exit 1
            fi
            echo "PASS: download-path matches specified path with merge-multiple=true"
          else
            # Action failed (expected without real GitHub API access in act environment)
            echo "INFO: Action failed as expected without real GitHub API credentials in act environment"
            echo "PASS: action failed gracefully with merge-multiple flag"
          fi
